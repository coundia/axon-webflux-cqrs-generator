package {{package}};

{{#imports}}
import {{.}};
{{/imports}}

public class {{name}}Mapper {

public static {{name}}Response toResponse({{name}} entity) {
return new {{name}}Response(
{{#fields}}            {{entity}}{{nameCapitalized}}.create(entity.get{{nameCapitalized}}()){{^last}},{{/last}}
{{/fields}}
);
}

public static {{name}} toEntity({{name}}Request request) {
return new {{name}}(
{{#fields}}            request.get{{nameCapitalized}}().value(){{^last}},{{/last}}
{{/fields}}
);
}
}
